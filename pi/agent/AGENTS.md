## Global Instructions

- use English in code comment and log
- chose language accordingly, if user ask in English, response in English; if asking in Chinese, response in Chinese
- always load and use `git-commit-message` skill before generate git commit message
- Do not automatically start the server, database, or launch the browser for testing and verification.
